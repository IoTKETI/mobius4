[
  {
    "cnd": "http://www.example.com/schema/parkingBlock.xsd",
    "xsd": "./parkingBlock.xsd",
    "description": "A URL-style containerDefinition: the identifier happens to be where the XSD lives. The xsd path is relative to THIS file's directory, so it resolves while the manifest sits here in docs/examples/specializations/. Copy the manifest to config/ and the path has to be rewritten -- see the README beside this file."
  }
]
